# 更新日志

本项目所有值得注意的变更都将记录在本文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。
本仓库目前没有 Git 标签，也没有 GitHub Release（已通过 `git tag` 与
`gh release list` 核实：均为空），因此按项目约定，下面这唯一一条目汇总首次发布时的
完整功能集。更细粒度的历史请直接查阅 Git log；待维护者打下首个标签后，本条目将拆分为逐版本小节。

## [Unreleased]

暂无。

## [0.2.0] - 2026-08-27

### 新增

- **高级选项**独立为「03 — 高级选项」区块，与基本字符类型开关明确分层：
  - **易朗读**——剔除读音易混淆的字符（大小写同剔的 `B C D E G P T V Z`、
    留 `M` 剔 `N`、数字 `1`/`7`（中文「一 / 七」）），符号收窄为可朗读子集
    `!@#$%*+=?`（池 94 → 49）。
  - **易复述**——按 4 位一组以连字符或下划线分段（二选一切换器）；分隔符
    纯属排版（不计熵）、绝不从候选池中抽取，复制得到的是分段后的字符串。

### 变更

- **「人类易读」更名「易输入」**，并从字符类型网格移入新的高级区块；过滤
  行为不变（`0Oo1lIi2Zz5Ss8B6b9gq`、仅清晰符号、池 94 → 55）。两个高级
  过滤可叠加（池 94 → 35）。

### 测试

- 生成器测试从 16 项扩展到 22 项（易朗读剔除、过滤叠加、分段排版、分隔符出池）。
- 新增 `test/app-test.js` —— 8 项 UI 接线测试：以最小 DOM 桩真实加载
  `app.js`/`i18n.js`/`generator.js`，覆盖选项开关、分隔符切换、分段显示、
  熵值读数与黑名单联动（全套共 43 项测试）。

## [0.1.0] - 2026-08-27

首次公开发布于 GitHub Pages。本条目汇总截至发布之日的完整功能集。

### 新增

- 密码生成核心：Web Crypto API 随机源 + 拒绝采样；大写 / 小写 / 数字 /
  符号四类字符，勾选即保证每类至少出现一个；Fisher–Yates 洗牌；纯函数
  UMD 模块，可在 Node 中直接测试。
- 长度控制 4–64 位，滑杆与数字输入框双向同步。
- 人类易读模式：剔除易混淆字符（`0Oo1lIi2Zz5Ss8B6b9gq`），符号仅保留清晰醒目的子集。
- 逐字符黑名单；被过滤排空的字符类自动跳过。
- 实时熵值显示（`长度 × log2(候选池大小)`）与四段强度条（40 / 60 / 80 比特分界）。
- 一键复制：剪贴板 API，失败时回退旧式方案。
- 中英双语界面：浏览器语言检测 + 选择持久化（localStorage 键 `pg-lang`）。
- 赞赏弹窗：支付宝 / 微信支付双标签页，二维码全部在浏览器本地实时生成（内联
  MIT 许可的 `qrcode-generator`，懒加载）；手机端支付宝直跳官方链接、按可见性
  智能回退二维码；完整无障碍支持（对话框 role、tablist、焦点圈闭、ESC /
  遮罩关闭、焦点归还）。
- 瑞士 / 德国学派视觉设计：白纸、近黑墨色、单一红色点缀、细线分隔，无渐变无阴影。
- Node 测试套件：生成器 16 项 + 赞赏契约 13 项。

（待首个 Git 标签创建后，将补充版本对比链接。）
